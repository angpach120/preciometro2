{
  "name": "my-react-app",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://TU_USUARIO.github.io/NOMBRE_DEL_REPO",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.23.0"
  },
  "scripts": {
    "start": "npx serve -s build",
    "build": "npx babel --presets @babel/preset-react src --out-dir build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "@babel/cli": "^7.24.0",
    "@babel/preset-react": "^7.22.15",
    "gh-pages": "^6.1.1"
  }
}
